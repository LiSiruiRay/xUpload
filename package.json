{
  "name": "xupload",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite build --watch && npm run postbuild",
    "build": "vite build && vite build -c vite.config.content.ts && npm run postbuild",
    "postbuild": "cp manifest.dist.json dist/manifest.json && cp src/content.css dist/content.css"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^5.4.0"
  }
}
